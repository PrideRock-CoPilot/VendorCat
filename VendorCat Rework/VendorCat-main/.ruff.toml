# Ruff configuration for VendorCatalog
# See: https://docs.astral.sh/ruff/

# Line length
line-length = 120

# Python version
target-version = "py311"

[lint]
# Enable rules
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort (import sorting)
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
]

# Ignore specific rules
ignore = [
    "E501",   # Line too long (handled by formatter)
    "B008",   # Do not perform function call in argument defaults
    "B904",   # Within an except clause, raise exceptions with raise ... from err
    "SIM108", # Use ternary operator (sometimes less readable)
]

# Exclude directories
exclude = [
    ".git",
    ".mypy_cache",
    ".pytest_cache",
    ".ruff_cache",
    ".venv",
    "__pycache__",
    "htmlcov",
    "build",
    "dist",
]

[lint.per-file-ignores]
# Allow unused imports in __init__.py
"__init__.py" = ["F401"]
# Allow print statements in scripts
"scripts/*.py" = ["T201"]
# Allow assert in tests
"tests/*" = ["S101"]

[lint.isort]
known-first-party = ["app", "vendor_catalog_app"]
section-order = ["future", "standard-library", "third-party", "first-party", "local-folder"]

[lint.mccabe]
# Maximum complexity
max-complexity = 10

# Custom rule: Detect SQL in routers (placeholder - requires custom plugin)
# This is a note for future enhancement
# Pattern to detect: SELECT|INSERT|UPDATE|DELETE in app/vendor_catalog_app/web/routers/
